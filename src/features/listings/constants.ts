export const DEFAULT_LISTINGS_LIMIT = 20;
/**
 * How many recent listings the empty state falls back to.
 *
 * A short run, not a second feed: it is a way out of a dead end, and a full
 * page of unrelated results would read as though the filters had been ignored
 * rather than as an offer.
 */
export const EMPTY_STATE_FALLBACK_LIMIT = 6;
export const MAX_LISTINGS_LIMIT = 50;
